function paragraphSelector() {
  var paragraphs = $('p');
  return paragraphs;
}

function lastImageSelector() {
  var lastImage = $('img:last');
  return lastImage;
}

function ninjaBabySelector() {
  var selectById = $('#baby-ninja');
  return selectById;
}

function divSelector() {
  var selectByClass = $('.pics');
  return selectByClass;
}

function firstListItem() {
  var lastPicInIndex = $('#pic-list li:first-child');
  return lastPicInIndex;
}
